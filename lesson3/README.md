# 📖 Урок 3: Гостевская книга + SQLite

Сохраняем записи в **базу данных SQLite**: имя, сообщение, дата. Данные не пропадают при перезапуске.

## Что нового

- **SQLite** — встроенная БД, файл `guestbook.db`.
- Таблица `entries`: `id`, `author`, `message`, `created_at`.
- Маршруты: главная, GET/POST `/api/entries`. Добавление записи и вывод всех.

## Как запустить

```bash
cd lesson3/guestbook-flask
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

Откройте http://localhost:5002

## Структура

```
lesson3/
├── README.md
└── guestbook-flask/
    ├── app.py
    ├── requirements.txt
    ├── templates/
    │   └── index.html
    └── static/
        ├── style.css
        └── script.js
```

После первого запуска появится файл `guestbook.db`.

## Что сделать самому

1. Создать таблицу при старте (`CREATE TABLE IF NOT EXISTS ...`).
2. При POST — `INSERT` в БД, вернуть новую запись в JSON.
3. При GET — `SELECT`, вернуть список записей в JSON.
4. Страница: форма (имя, сообщение) и блок с записями, обновление через `fetch`.
