# 📋 Урок 2: Список дел (To-Do)

Усложняем проект: **несколько маршрутов**, **список данных в памяти**, добавление, удаление и отметка «сделано».

## Что нового по сравнению с уроком 1

- Несколько маршрутов: `/`, `/api/todos`, `/api/todos/<id>/toggle`, `/api/todos/<id>` (DELETE).
- Список дел хранится в памяти (при перезапуске сервера пропадает).
- Динамический список на странице: добавляем, удаляем, отмечаем дела без перезагрузки.

## Как запустить эталонный проект

```bash
cd lesson2/todo-flask
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

Откройте http://localhost:5001

## Структура проекта

```
lesson2/
├── README.md
└── todo-flask/
    ├── app.py
    ├── requirements.txt
    ├── templates/
    │   └── index.html
    └── static/
        ├── style.css
        └── script.js
```

## Что сделать самому

1. Создать проект в PyCharm, установить Flask.
2. Реализовать хранение списка в переменной (как в эталоне).
3. Сделать маршруты: главная страница, GET/POST для списка дел, toggle и delete по `id`.
4. Сверстать страницу и подключить JS: загрузка списка, добавление, удаление, отметка «готово».

## Подсказки

- У каждого дела есть `id`, `text`, `done`. Id удобно выдавать счётчиком.
- Удаление и toggle делай через `fetch` с `method: 'DELETE'` и `method: 'POST'` на соответствующие URL.
