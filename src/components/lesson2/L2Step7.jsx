function L2Step7() {
  return (
    <div className="step-content trainer-step">
      <h2>Шаг 7: Реализация удаления (DELETE)</h2>

      <div className="info-box trainer-definition">
        <strong>💡 Что такое DELETE запрос?</strong>
        <p style={{marginTop: '10px'}}>
          DELETE — это специальный тип запроса для удаления чего-то. Когда ты нажимаешь кнопку "Удалить", 
          JavaScript отправляет DELETE запрос на сервер с номером дела, которое нужно удалить.
        </p>
        <p style={{marginTop: '10px'}}>
          Сервер получает этот запрос и удаляет дело из списка. Это как вырвать страницу из блокнота — 
          дело больше не существует в списке.
        </p>
      </div>

      <p>Реализуем маршрут, который удаляет задачу по её идентификатору.</p>

      <div className="code-block">
        <code>{`@app.route('/api/todos/<int:id>', methods=['DELETE'])
def delete_todo(id):
    global todos
    # Формируем новый список без задачи с этим id
    new_todos = [t for t in todos if t['id'] != id]
    if len(new_todos) == len(todos):
        return jsonify({'success': False, 'error': 'not_found'}), 404
    todos = new_todos
    return jsonify({'success': True})`}</code>
      </div>

      <h3>🔍 Пояснение для тебя</h3>
      <ul>
        <li><code>@app.route('/api/todos/&lt;int:id&gt;', methods=['DELETE'])</code> — создаём маршрут для удаления. Метод <code>DELETE</code> означает "удалить что-то".</li>
        <li><code>def delete_todo(id):</code> — функция получает номер дела, которое нужно удалить.</li>
        <li><code>global todos</code> — говорим Python, что мы хотим изменить глобальную переменную <code>todos</code>.</li>
        <li><code>new_todos = [t for t in todos if t['id'] != id]</code> — создаём новый список, в котором есть все дела, КРОМЕ того, которое нужно удалить. Это называется "list comprehension" — короткий способ создать список. Читается так: "возьми все дела t из todos, если номер дела t не равен id".</li>
        <li><code>if len(new_todos) == len(todos):</code> — проверяем, изменилась ли длина списка. Если длина не изменилась, значит дела с таким номером не было (ничего не удалили).</li>
        <li><code>return jsonify</code> с <code>'success': False, 'error': 'not_found'</code> и кодом 404 — если дело не найдено, возвращаем ошибку 404.</li>
        <li><code>todos = new_todos</code> — заменяем старый список новым (без удалённого дела).</li>
        <li><code>return jsonify</code> с <code>'success': True</code> — возвращаем успешный ответ. Дело удалено!</li>
      </ul>
      <div className="info-box" style={{marginTop: '20px'}}>
        <strong>💡 Почему создаём новый список?</strong>
        <p style={{marginTop: '10px'}}>
          Мы создаём новый список без удалённого элемента, а не удаляем напрямую. Это безопаснее и проще понять. 
          В реальных больших проектах часто используют базы данных, где удаление работает по-другому, 
          но для нашего урока этот способ идеален!
        </p>
      </div>

      <h3>Тест</h3>
      <pre>{`curl -X DELETE http://localhost:5001/api/todos/1`}</pre>

      <div className="success-box">
        <strong>✅ После этого:</strong> задача больше не должна появляться в GET /api/todos.
      </div>
    </div>
  )
}

export default L2Step7

