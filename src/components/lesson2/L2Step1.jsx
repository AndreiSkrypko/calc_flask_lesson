function L2Step1() {
  return (
    <div className="step-content trainer-step">
      <h2>Шаг 1: Подготовка и первый маршрут</h2>

      <h3>Что ты сделаешь</h3>
      <ol>
        <li>Создашь папку проекта и виртуальное окружение (venv).</li>
        <li>Установишь Flask командой <code>pip install flask</code>.</li>
        <li>Создадим файл <code>app.py</code> и запустим сервер.</li>
      </ol>

      <div className="info-box trainer-definition">
        <strong>💡 Зачем нужен сервер?</strong>
        <p style={{marginTop: '10px'}}>
          Представь, что у тебя есть блокнот со списком дел. Ты можешь его читать и писать в нём. 
          Но что если твой друг хочет посмотреть твой список? Ему нужен доступ к этому блокноту.
        </p>
        <p style={{marginTop: '10px'}}>
          <strong>Сервер</strong> — это как твой блокнот, который лежит в специальном месте (на компьютере), 
          и к нему могут обращаться другие программы (например, браузер). Когда ты открываешь страницу в браузере, 
          браузер "просит" сервер: "Дай мне список дел!", и сервер отвечает: "Вот список!".
        </p>
        <p style={{marginTop: '10px'}}>
          <strong>Flask</strong> — это специальная программа на Python, которая умеет создавать такие серверы. 
          Она слушает запросы от браузера и отвечает на них.
        </p>
      </div>

      <h3>Скопируй в <code>app.py</code> (минимум)</h3>
      <div className="code-block">
        <code>{`from flask import Flask, jsonify

app = Flask(__name__)

todos = []  # пока пустой список

@app.route('/api/todos')
def get_todos():
    return jsonify({'todos': todos})

if __name__ == '__main__':
    app.run(port=5001, debug=True)`}</code>
      </div>

      <h3>🔍 Разберём код по строчкам:</h3>
      <ul>
        <li><code>from flask import Flask, jsonify</code> — подключаем библиотеку Flask. Это как взять инструмент из ящика с инструментами.</li>
        <li><code>app = Flask(__name__)</code> — создаём приложение Flask. Это как включить сервер и сказать: "Готов принимать запросы!"</li>
        <li><code>todos = []</code> — создаём пустой список. Это наш "блокнот", где будут храниться дела. Пока он пустой.</li>
        <li><code>@app.route('/api/todos')</code> — создаём "адрес" на сервере. Когда кто-то обращается к адресу <code>/api/todos</code>, выполнится функция <code>get_todos()</code>.</li>
        <li><code>def get_todos():</code> — функция, которая возвращает список дел. Она говорит: "Вот все дела из списка!"</li>
        <li><code>return jsonify</code> с <code>'todos': todos</code> — превращаем список в формат JSON (это специальный формат, который понимает браузер) и отправляем его.</li>
        <li><code>app.run(port=5001, debug=True)</code> — запускаем сервер на порту 5001. Порт — это как номер двери, через которую можно попасть к серверу.</li>
      </ul>

      <div className="info-box" style={{marginTop: '20px'}}>
        <strong>🎯 Что происходит, когда ты открываешь адрес в браузере:</strong>
        <ol style={{marginTop: '10px'}}>
          <li>Браузер отправляет запрос на адрес <code>http://localhost:5001/api/todos</code></li>
          <li>Сервер видит, что запрос идёт на маршрут <code>/api/todos</code></li>
          <li>Сервер вызывает функцию <code>get_todos()</code></li>
          <li>Функция берёт список <code>todos</code> и возвращает его в формате JSON</li>
          <li>Браузер получает ответ и показывает его тебе</li>
        </ol>
      </div>

      <div className="success-box">
        <strong>✅ Проверь сам:</strong> 
        <ol style={{marginTop: '10px'}}>
          <li>Запусти <code>python app.py</code> в терминале</li>
          <li>Открой в браузере <code>http://localhost:5001/api/todos</code></li>
          <li>Ты должен увидеть: <code>"todos": []</code> в фигурных скобках — это пустой список дел в формате JSON!</li>
        </ol>
      </div>
    </div>
  )
}

export default L2Step1
