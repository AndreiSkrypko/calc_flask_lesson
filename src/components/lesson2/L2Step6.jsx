function L2Step6() {
  return (
    <div className="step-content trainer-step">
      <h2>Шаг 6: Реализация toggle — пометить как выполнено</h2>

      <div className="info-box trainer-definition">
        <strong>💡 Что такое toggle?</strong>
        <p style={{marginTop: '10px'}}>
          Toggle — это переключатель. Представь выключатель света: один раз нажал — свет включился, 
          второй раз нажал — свет выключился. То же самое с делами: один раз нажал "Готово" — дело выполнено, 
          второй раз нажал — дело снова не выполнено.
        </p>
        <p style={{marginTop: '10px'}}>
          В коде это делается просто: <code>t['done'] = not t['done']</code> — меняем значение на противоположное.
          Если было <code>False</code>, станет <code>True</code>, и наоборот.
        </p>
      </div>

      <p>Добавим маршрут, который переключает поле <code>done</code> у задачи.</p>

      <div className="code-block">
        <code>{`@app.route('/api/todos/<int:id>/toggle', methods=['POST'])
def toggle_todo(id):
    # Итерируем по списку и ищем задачу с нужным id
    for t in todos:
        if t['id'] == id:
            t['done'] = not t['done']  # меняем True <-> False
            return jsonify({'success': True, 'todo': t})
    # Если не нашли — возвращаем 404
    return jsonify({'success': False, 'error': 'not_found'}), 404`}</code>
      </div>

      <h3>🔍 Пояснение для тебя</h3>
      <ul>
        <li><code>@app.route('/api/todos/&lt;int:id&gt;/toggle', methods=['POST'])</code> — создаём маршрут с параметром <code>id</code>. Когда браузер отправит запрос на <code>/api/todos/1/toggle</code>, Flask автоматически передаст число 1 в функцию как параметр <code>id</code>.</li>
        <li><code>def toggle_todo(id):</code> — функция получает номер дела, которое нужно переключить.</li>
        <li><code>for t in todos:</code> — проходим по всем делам в списке, чтобы найти нужное.</li>
        <li><code>if t['id'] == id:</code> — проверяем, совпадает ли номер дела с тем, что нам нужен.</li>
        <li><code>t['done'] = not t['done']</code> — меняем статус на противоположный. <code>not</code> — это оператор "не": <code>not True</code> = <code>False</code>, <code>not False</code> = <code>True</code>.</li>
        <li><code>return jsonify</code> с <code>'success': True, 'todo': t</code> — возвращаем изменённое дело, чтобы браузер мог обновить страницу.</li>
        <li><code>return jsonify</code> с <code>'success': False, 'error': 'not_found'</code> и кодом 404 — если дело не найдено, возвращаем код 404 (Not Found — не найдено). Это стандартный код для "такого не существует".</li>
      </ul>

      <h3>Короткий тест</h3>
      <pre>{`curl -X POST http://localhost:5001/api/todos/1/toggle`}</pre>

      <div className="success-box">
        <strong>✅ После применения:</strong> при GET /api/todos у задачи изменится поле <code>done</code>.
      </div>
    </div>
  )
}

export default L2Step6

