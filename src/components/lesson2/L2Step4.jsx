function L2Step4() {
  return (
    <div className="step-content trainer-step">
      <h2>Шаг 4: Тестируем и улучшаем</h2>

      <h3>Запуск</h3>
      <ol>
        <li>Включи виртуальное окружение и запусти: <code>python app.py</code>.</li>
        <li>Открой в браузере: <code>http://localhost:5001</code>.</li>
      </ol>

      <h3>Проверяй сам</h3>
      <ul>
        <li>Введи текст и нажми «Добавить» — появилась ли задача?</li>
        <li>Попробуй добавить пустую строку — ничего не должно добавиться.</li>
        <li>Нажми «Готово» — задача должна пометиться (например, ✅ или зачёркнуться).</li>
        <li>Нажми «Удалить» — задача должна исчезнуть.</li>
      </ul>

      <div className="info-box">
        <strong>💡 Почему это важно</strong>
        <p style={{marginTop: '10px'}}>
          Представь, что ты строишь дом из кубиков. Если ты проверяешь каждый кубик после того, как положил его, 
          то если что-то не так, ты сразу видишь проблему и можешь её исправить. 
          Но если ты сложишь весь дом, а потом поймёшь, что где-то ошибка — будет сложно найти, где именно проблема!
        </p>
        <p style={{marginTop: '10px'}}>
          То же самое с кодом: если что‑то не работает, легче исправить маленький кусочек кода, чем весь проект.
          Поэтому мы проверяем каждый шаг сразу после того, как его сделали.
        </p>
      </div>

      <h3>Идеи для улучшения</h3>
      <ul>
        <li>Сохрани задачи в файл или в SQLite (будет в уроке 3).</li>
        <li>Добавь красивые стили и анимации.</li>
        <li>Добавь проверку длины и запрещай пустые задачи.</li>
      </ul>

      <h3>🎯 Как всё работает вместе — простое объяснение</h3>
      
      <div className="info-box trainer-definition" style={{marginTop: '20px', marginBottom: '20px'}}>
        <strong>💡 Представь ресторан с официантом:</strong>
        <ol style={{marginTop: '15px'}}>
          <li><strong>Ты (клиент)</strong> — сидишь за столом и видишь меню (страницу в браузере). Ты хочешь заказать блюдо (добавить дело).</li>
          <li><strong>Официант (JavaScript)</strong> — подходит к тебе, записывает заказ и относит его на кухню (отправляет запрос на сервер).</li>
          <li><strong>Кухня (Flask сервер)</strong> — получает заказ, готовит блюдо (обрабатывает данные) и отдаёт его официанту.</li>
          <li><strong>Официант</strong> — приносит готовое блюдо обратно тебе (JavaScript получает ответ и показывает его на странице).</li>
        </ol>
      </div>

      <div className="info-box" style={{marginTop: '20px', marginBottom: '20px'}}>
        <strong>📋 Что происходит, когда ты добавляешь дело:</strong>
        <ol style={{marginTop: '15px'}}>
          <li><strong>Ты нажимаешь кнопку "Добавить"</strong> на странице</li>
          <li><strong>JavaScript "ловит" это нажатие</strong> — функция <code>addEventListener</code> говорит: "Ага, кнопку нажали!"</li>
          <li><strong>JavaScript берёт текст</strong> из поля ввода (например, "Купить молоко")</li>
          <li><strong>JavaScript отправляет запрос</strong> на сервер через <code>fetch('/api/todos', {method: 'POST', ...})</code></li>
          <li><strong>Сервер получает запрос</strong> — Flask видит, что пришёл POST запрос на адрес <code>/api/todos</code></li>
          <li><strong>Сервер вызывает функцию</strong> <code>add_todo()</code> — она создаёт новое дело и добавляет его в список <code>todos</code></li>
          <li><strong>Сервер отправляет ответ</strong> обратно: "Всё хорошо, дело добавлено!"</li>
          <li><strong>JavaScript получает ответ</strong> и вызывает <code>loadTodos()</code> — эта функция загружает весь список дел заново</li>
          <li><strong>Список обновляется на странице</strong> — ты видишь новое дело!</li>
        </ol>
      </div>

      <div className="info-box" style={{marginTop: '20px', marginBottom: '20px'}}>
        <strong>🔄 Что происходит, когда ты отмечаешь дело как выполненное:</strong>
        <ol style={{marginTop: '15px'}}>
          <li><strong>Ты нажимаешь "Готово"</strong> рядом с делом</li>
          <li><strong>JavaScript отправляет POST запрос</strong> на <code>/api/todos/1/toggle</code> (где 1 — это номер дела)</li>
          <li><strong>Сервер находит дело</strong> с номером 1 в списке</li>
          <li><strong>Сервер меняет статус</strong> — если было <code>done: false</code>, становится <code>done: true</code></li>
          <li><strong>Сервер отправляет ответ</strong> с изменённым делом</li>
          <li><strong>JavaScript обновляет список</strong> — дело теперь отмечено как выполненное (✅ или зачёркнуто)</li>
        </ol>
      </div>

      <div className="info-box" style={{marginTop: '20px', marginBottom: '20px'}}>
        <strong>🗑️ Что происходит, когда ты удаляешь дело:</strong>
        <ol style={{marginTop: '15px'}}>
          <li><strong>Ты нажимаешь "Удалить"</strong> (✕) рядом с делом</li>
          <li><strong>JavaScript отправляет DELETE запрос</strong> на <code>/api/todos/1</code> (где 1 — номер дела)</li>
          <li><strong>Сервер находит дело</strong> с номером 1</li>
          <li><strong>Сервер удаляет его</strong> из списка — создаёт новый список без этого дела</li>
          <li><strong>Сервер отправляет ответ</strong>: "Удалено успешно!"</li>
          <li><strong>JavaScript удаляет элемент</strong> со страницы — дело исчезает из списка</li>
        </ol>
      </div>

      <div className="info-box" style={{marginTop: '20px', marginBottom: '20px', background: '#fff3cd', borderLeftColor: '#ffc107'}}>
        <strong>⚠️ Важно: JavaScript и HTML — это фронтенд, а не бэкенд!</strong>
        <p style={{marginTop: '15px'}}>
          <strong>Фронтенд (клиент) состоит из трёх частей:</strong>
        </p>
        <div style={{marginTop: '15px', padding: '15px', background: '#f8f9fa', borderRadius: '8px'}}>
          <ul style={{marginTop: '10px', marginLeft: '20px'}}>
            <li><strong>HTML</strong> — это "скелет" страницы, структура:
              <ul style={{marginTop: '5px', marginLeft: '20px'}}>
                <li>Определяет, что будет на странице (кнопки, поля ввода, списки)</li>
                <li>Это как каркас дома — показывает, где что находится</li>
                <li>Работает в браузере</li>
                <li>Пример: <code>&lt;button id="addBtn"&gt;Добавить&lt;/button&gt;</code> — создаёт кнопку</li>
              </ul>
            </li>
            <li style={{marginTop: '15px'}}><strong>CSS</strong> — это "одежда" страницы, стили:
              <ul style={{marginTop: '5px', marginLeft: '20px'}}>
                <li>Определяет, как всё выглядит (цвета, размеры, расположение)</li>
                <li>Это как краска и обои для дома — делает его красивым</li>
                <li>Работает в браузере</li>
              </ul>
            </li>
            <li style={{marginTop: '15px'}}><strong>JavaScript</strong> — это "мозги" страницы, логика:
              <ul style={{marginTop: '5px', marginLeft: '20px'}}>
                <li>Делает страницу "живой" — реагирует на клики, отправляет запросы</li>
                <li>Это как электричество в доме — заставляет всё работать</li>
                <li>Работает в браузере</li>
                <li>Отправляет запросы на сервер и обновляет HTML</li>
              </ul>
            </li>
          </ul>
        </div>
        <p style={{marginTop: '15px'}}>
          <strong>Бэкенд — это Flask (Python)</strong> — код на Python выполняется на сервере (на компьютере, где запущен <code>python app.py</code>).
        </p>
        <div style={{marginTop: '15px', padding: '15px', background: '#e7f3ff', borderRadius: '8px'}}>
          <strong>📊 Полное разделение ролей:</strong>
          <ul style={{marginTop: '10px', marginLeft: '20px'}}>
            <li><strong>Фронтенд (работает в браузере):</strong>
              <ul style={{marginTop: '5px', marginLeft: '20px'}}>
                <li><strong>HTML</strong> — структура страницы (что видно)</li>
                <li><strong>CSS</strong> — стили страницы (как выглядит)</li>
                <li><strong>JavaScript</strong> — логика страницы (что происходит при кликах)</li>
                <li>Отправляет запросы на сервер через <code>fetch</code></li>
                <li>НЕ хранит данные постоянно</li>
              </ul>
            </li>
            <li style={{marginTop: '15px'}}><strong>Бэкенд (работает на сервере):</strong>
              <ul style={{marginTop: '5px', marginLeft: '20px'}}>
                <li><strong>Flask (Python)</strong> — обрабатывает запросы</li>
                <li>Хранит данные (список дел в памяти)</li>
                <li>Возвращает ответы в формате JSON</li>
                <li>Выполняет бизнес-логику (добавление, удаление, изменение дел)</li>
              </ul>
            </li>
          </ul>
        </div>
        <p style={{marginTop: '15px'}}>
          <strong>Аналогия с рестораном:</strong>
        </p>
        <ul style={{marginTop: '10px', marginLeft: '20px'}}>
          <li><strong>HTML</strong> — это столы и стулья в зале (структура ресторана)</li>
          <li><strong>CSS</strong> — это украшения и освещение (как выглядит зал)</li>
          <li><strong>JavaScript</strong> — это официант (принимает заказы, приносит еду)</li>
          <li><strong>Flask</strong> — это кухня (готовит еду, хранит продукты)</li>
        </ul>
        <p style={{marginTop: '15px'}}>
          Когда ты открываешь страницу, браузер загружает HTML, CSS и JavaScript. 
          JavaScript "оживляет" страницу и может общаться с сервером (Flask), чтобы получать и отправлять данные.
        </p>
      </div>

      <div className="success-box" style={{marginTop: '20px'}}>
        <strong>💡 Главное, что нужно понять:</strong>
        <ul style={{marginTop: '10px'}}>
          <li><strong>Бэкенд (Flask/Python)</strong> — хранит данные (список дел) и обрабатывает запросы. Работает на сервере.</li>
          <li><strong>Фронтенд (JavaScript)</strong> — отправляет запросы и обновляет страницу. Работает в браузере.</li>
          <li><strong>API (маршруты)</strong> — это "адреса", по которым фронтенд может обратиться к бэкенду</li>
          <li><strong>Все действия происходят без перезагрузки страницы</strong> — это делает JavaScript через <code>fetch</code>, общаясь с бэкендом</li>
        </ul>
      </div>

      <div className="success-box trainer-examples">
        <strong>✅ Урок 2 пройден!</strong>
        <p>Примеры HTML/JS файлов находятся в <code>lesson2/todo-flask/templates/index.html</code> и <code>lesson2/todo-flask/static/script.js</code>. Сравнивай свой код с ними.</p>
      </div>
      
      <h3>Контрольный список — какие файлы должны быть</h3>
      <p>Проверь, что в твоём проекте созданы следующие файлы и папки:</p>
      <div className="code-block">
        <code>{`project-folder/
├─ app.py                # сервер на Flask
├─ requirements.txt      # pip install -r requirements.txt (опционально)
├─ templates/
│  └─ index.html         # HTML-шаблон страницы
└─ static/
   ├─ script.js          # клиентский JavaScript (fetch, обработчики)
   └─ style.css          # стили для страницы`}</code>
      </div>

      <p>Команды для проверки:</p>
      <ul>
        <li>Создай виртуальное окружение: <code>python -m venv venv</code> и активируй его.</li>
        <li>Установи зависимости (если есть): <code>pip install -r requirements.txt</code>.</li>
        <li>Запусти сервер: <code>python app.py</code> — открой <code>http://localhost:5001</code>.</li>
      </ul>
    </div>
  )
}

export default L2Step4
