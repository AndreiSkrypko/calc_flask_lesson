function L2Step3() {
  return (
    <div className="step-content trainer-step">
      <h2>Шаг 3: Страница и простой JavaScript</h2>

      <div className="info-box trainer-definition">
        <strong>💡 Зачем нужен JavaScript?</strong>
        <p style={{marginTop: '10px'}}>
          HTML — это как картинка: она показывает, что должно быть на странице, но не может ничего делать сама по себе.
          JavaScript — это как "мозги" страницы: он может реагировать на твои действия (клики, ввод текста) и общаться с сервером.
        </p>
        <p style={{marginTop: '10px'}}>
          Представь:
        </p>
        <ul style={{marginTop: '10px'}}>
          <li><strong>HTML</strong> — это статичная картинка меню в ресторане</li>
          <li><strong>JavaScript</strong> — это официант, который берёт твой заказ, относит его на кухню (сервер) и приносит блюдо обратно</li>
          <li><strong>Сервер (Flask)</strong> — это кухня, где готовят блюдо (обрабатывают данные)</li>
        </ul>
        <p style={{marginTop: '10px'}}>
          Без JavaScript страница была бы "мёртвой" — ты мог бы только смотреть на неё, но не мог бы ничего делать.
          С JavaScript страница становится "живой" — ты можешь нажимать кнопки, и что-то будет происходить!
        </p>
      </div>

      <h3>1) HTML — сделай так</h3>
      <div className="code-block">
        <code>{`<!-- templates/index.html -->
<input id="newText" placeholder="Что нужно сделать?" />
<button id="addBtn">Добавить</button>
<ul id="todosList"></ul>`}</code>
      </div>
      <p>Скопируй этот HTML — это поле ввода, кнопка и пустой список.</p>
      <div className="info-box">
        <strong>📝 Что это:</strong>
        <ul style={{marginTop: '10px'}}>
          <li><code>&lt;input id="newText"&gt;</code> — поле для ввода текста. У него есть ID, чтобы JavaScript мог его найти.</li>
          <li><code>&lt;button id="addBtn"&gt;</code> — кнопка "Добавить". JavaScript будет слушать, когда на неё нажмут.</li>
          <li><code>&lt;ul id="todosList"&gt;</code> — пустой список. JavaScript будет добавлять в него дела.</li>
        </ul>
      </div>

      <h3>2) JavaScript — загрузи и покажи список</h3>
      <div className="code-block">
        <code>{`// static/script.js
async function loadTodos() {
  const res = await fetch('/api/todos')
  const data = await res.json()
  const ul = document.getElementById('todosList')
  ul.innerHTML = ''
  data.todos.forEach(t => {
    const li = document.createElement('li')
    li.textContent = t.text + (t.done ? ' ✅' : '')
    ul.appendChild(li)
  })
}
document.addEventListener('DOMContentLoaded', loadTodos)`}</code>
      </div>

      <h3>🔍 Разберём код загрузки списка:</h3>
      <ul>
        <li><code>async function loadTodos()</code> — создаём функцию для загрузки списка. <code>async</code> означает, что функция может ждать ответа от сервера.</li>
        <li><code>const res = await fetch('/api/todos')</code> — отправляем запрос на сервер. <code>fetch</code> — это как "посыльный", который идёт к серверу и приносит ответ. <code>await</code> означает "подожди, пока посыльный вернётся".</li>
        <li><code>const data = await res.json()</code> — превращаем ответ сервера в понятный формат (JSON → объект JavaScript).</li>
        <li><code>const ul = document.getElementById('todosList')</code> — находим список на странице по его ID.</li>
        <li><code>ul.innerHTML = ''</code> — очищаем список, чтобы нарисовать его заново.</li>
        <li><code>data.todos.forEach(t => ...)</code> — проходим по каждому делу из списка. Внутри функции мы создаём элемент для каждого дела.</li>
        <li><code>const li = document.createElement('li')</code> — создаём новый элемент списка.</li>
        <li><code>li.textContent = t.text + (t.done ? ' ✅' : '')</code> — записываем текст дела и добавляем галочку, если дело выполнено.</li>
        <li><code>ul.appendChild(li)</code> — добавляем элемент в список на странице.</li>
        <li><code>document.addEventListener('DOMContentLoaded', loadTodos)</code> — когда страница загрузится, сразу вызываем функцию загрузки списка.</li>
      </ul>

      <h3>3) Добавление задачи</h3>
      <div className="code-block">
        <code>{`document.getElementById('addBtn').addEventListener('click', async () => {
  const text = document.getElementById('newText').value
  await fetch('/api/todos', { 
    method: 'POST', 
    headers: {'Content-Type':'application/json'}, 
    body: JSON.stringify({ text }) 
  })
  loadTodos()
})`}</code>
      </div>

      <h3>🔍 Разберём код добавления:</h3>
      <ul>
        <li><code>document.getElementById('addBtn')</code> — находим кнопку "Добавить" на странице.</li>
        <li><code>.addEventListener('click', ...)</code> — говорим: "Когда на эту кнопку нажмут, выполни эту функцию".</li>
        <li><code>const text = document.getElementById('newText').value</code> — берём текст, который пользователь ввёл в поле.</li>
        <li><code>await fetch('/api/todos', ...)</code> с методом POST — отправляем POST запрос на сервер. Это как сказать серверу: "Добавь новое дело!"</li>
        <li><code>headers</code> с <code>'Content-Type':'application/json'</code> — говорим серверу: "Мы отправляем данные в формате JSON".</li>
        <li><code>body: JSON.stringify</code> с объектом <code>text</code> — превращаем объект JavaScript в строку JSON и отправляем её серверу.</li>
        <li><code>loadTodos()</code> — после добавления загружаем список заново, чтобы увидеть новое дело.</li>
      </ul>

      <div className="info-box" style={{marginTop: '20px'}}>
        <strong>🎯 Как это работает вместе:</strong>
        <ol style={{marginTop: '10px'}}>
          <li>Ты нажимаешь кнопку "Добавить"</li>
          <li>JavaScript "ловит" это нажатие (addEventListener)</li>
          <li>JavaScript берёт текст из поля ввода</li>
          <li>JavaScript отправляет запрос на сервер через fetch</li>
          <li>Сервер получает запрос, добавляет дело в список</li>
          <li>Сервер отправляет ответ обратно</li>
          <li>JavaScript получает ответ и обновляет список на странице</li>
          <li>Ты видишь новое дело в списке!</li>
        </ol>
        <p style={{marginTop: '10px'}}>
          <strong>Важно:</strong> всё это происходит без перезагрузки страницы! Страница остаётся той же, только список обновляется.
        </p>
      </div>

      <div className="info-box">
        <strong>💡 Почему это круто:</strong>
        <p style={{marginTop: '10px'}}>
          Раньше, чтобы добавить что-то на сайт, нужно было перезагрузить всю страницу. 
          Это было медленно и неудобно. Теперь с JavaScript мы можем обновлять только нужную часть страницы — быстро и удобно!
        </p>
      </div>

      <div className="info-box">
        <strong>Совет:</strong> сначала убедись, что кнопка «Добавить» работает, потом добавим «Готово» и «Удалить» рядом с каждой задачей.
      </div>

      <div className="success-box">
        <strong>✅ Готово.</strong> Если всё работает — переходи к следующему шагу.
      </div>
    </div>
  )
}

export default L2Step3
