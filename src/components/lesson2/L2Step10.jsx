function L2Step10() {
  return (
    <div className="step-content trainer-step">
      <h2>Шаг 10: Валидация, коды ответов и улучшения</h2>

      <p>Закрепим хорошие практики: что тебе важно проверять и как возвращать понятные ответы.</p>

      <div className="code-block">
        <code>{`# В app.py — примеры ответов и обработка ошибок
from flask import make_response

@app.errorhandler(500)
def internal_error(e):
    return jsonify({'success': False, 'error': 'server_error'}), 500

# Внутри add_todo мы уже возвращаем 400 для пустого текста и 201 для создания.
# В toggle/delete возвращаем 404, если не найдено.

# Можно также добавить ограничение длины:
if len(text) > 200:
    return jsonify({'success': False, 'error': 'too_long'}), 400`}</code>
      </div>

      <h3>Идеи для улучшения (чуть сложнее)</h3>
      <ul>
        <li>Сохраняй задачи в файл или в SQLite, чтобы они не пропадали при перезапуске сервера.</li>
        <li>Добавь подтверждение перед удалением, чтобы не удалять случайно.</li>
        <li>Добавь сортировку и фильтр «только неготовые».</li>
      </ul>

      <div className="success-box">
        <strong>🎉 Отлично!</strong>
        <p>У тебя есть пошаговый урок. Если хочешь — скопируй готовые файлы из шага «Готовые файлы» или скажи, и я создам их прямо в проекте.</p>
      </div>
    </div>
  )
}

export default L2Step10

