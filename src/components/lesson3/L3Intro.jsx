function L3Intro() {
  return (
    <div className="step-content trainer-step">
      <h2>📖 Урок 3: Гостевская книга + SQLite</h2>

      <div className="info-box trainer-definition">
        <strong>💡 Что такое гостевская книга?</strong>
        <p style={{marginTop: '10px'}}>
          <strong>Гостевская книга</strong> — это страница, где посетители могут оставить своё имя и сообщение.
          Это как книга отзывов в ресторане: каждый гость может написать что-то, и все записи остаются навсегда.
        </p>
        <p style={{marginTop: '10px'}}>
          В отличие от урока 2, где данные хранились в памяти (и пропадали при перезапуске сервера), 
          здесь мы будем использовать <strong>базу данных SQLite</strong> — все записи сохраняются в файл 
          <code>guestbook.db</code> и не пропадают даже после перезапуска!
        </p>
      </div>

      <h3>🆕 Что нового в этом уроке</h3>
      <ul>
        <li><strong>SQLite</strong> — встроенная база данных Python. Не нужно устанавливать отдельный сервер БД — всё работает из файла!</li>
        <li><strong>Таблица <code>entries</code></strong> с полями:
          <ul>
            <li><code>id</code> — уникальный номер записи (автоматически увеличивается)</li>
            <li><code>author</code> — имя автора записи</li>
            <li><code>message</code> — текст сообщения</li>
            <li><code>created_at</code> — дата и время создания (ставится автоматически)</li>
          </ul>
        </li>
        <li><strong>API маршруты:</strong>
          <ul>
            <li><code>GET /api/entries</code> — получить все записи</li>
            <li><code>POST /api/entries</code> — добавить новую запись</li>
          </ul>
        </li>
      </ul>

      <div className="info-box trainer-definition">
        <strong>💡 Зачем нужна база данных?</strong>
        <p style={{marginTop: '10px'}}>
          Представь, что у тебя есть блокнот, где ты записываешь важные дела. 
          Если ты потеряешь блокнот или он порвётся — все записи пропадут!
        </p>
        <p style={{marginTop: '10px'}}>
          <strong>База данных</strong> — это как надёжный сейф для твоих данных. 
          Даже если ты перезапустишь компьютер или сервер, все записи останутся на месте, 
          потому что они хранятся в файле на диске.
        </p>
        <p style={{marginTop: '10px'}}>
          <strong>SQLite</strong> — это особая база данных, которая хранится в одном файле 
          (например, <code>guestbook.db</code>). Это очень удобно для небольших проектов — 
          не нужно устанавливать сложные программы, всё работает "из коробки"!
        </p>
      </div>

      <div className="success-box trainer-examples">
        <strong>🎯 Что мы сделаем:</strong>
        <ol style={{marginTop: '10px'}}>
          <li>Создадим базу данных SQLite и таблицу для записей</li>
          <li>Настроим функции для работы с базой данных</li>
          <li>Создадим API маршруты для получения и добавления записей</li>
          <li>Сделаем красивую форму для ввода имени и сообщения</li>
          <li>Напишем JavaScript для загрузки и отображения записей</li>
          <li>Добавим стили, чтобы всё выглядело красиво</li>
        </ol>
      </div>

      <h3>📁 Эталонный проект</h3>
      <p>
        Готовый проект находится в папке <code>lesson3/guestbook-flask</code>. 
        Чтобы запустить его:
      </p>
      <ol>
        <li>Перейди в папку проекта</li>
        <li>Установи зависимости: <code>pip install -r requirements.txt</code></li>
        <li>Запусти сервер: <code>python app.py</code></li>
        <li>Открой в браузере: <code>http://localhost:5002</code></li>
      </ol>

      <div className="info-box" style={{marginTop: '20px'}}>
        <strong>🎓 Что ты узнаешь:</strong>
        <ul style={{marginTop: '10px'}}>
          <li>Как работать с базами данных SQLite в Python</li>
          <li>Как создавать таблицы и выполнять SQL запросы</li>
          <li>Как сохранять данные так, чтобы они не пропадали</li>
          <li>Как создавать формы для ввода данных</li>
          <li>Как отображать данные из базы на странице</li>
        </ul>
      </div>

      <div className="success-box">
        <strong>✅ Готов начать?</strong> Переходи к следующему шагу — мы начнём с подготовки проекта и настройки базы данных!
      </div>
    </div>
  )
}

export default L3Intro
