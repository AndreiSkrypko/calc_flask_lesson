function L3Step5() {
  return (
    <div className="step-content trainer-step">
      <h2>Шаг 5: HTML шаблон — форма и контейнер для записей</h2>

      <div className="info-box trainer-definition">
        <strong>💡 Что такое HTML шаблон?</strong>
        <p style={{marginTop: '10px'}}>
          <strong>HTML шаблон</strong> — это структура страницы. Это как каркас дома: 
          он определяет, где будет форма для ввода, где будет список записей, но сам по себе не делает страницу "живой".
        </p>
        <p style={{marginTop: '10px'}}>
          Мы создадим форму с двумя полями (имя и сообщение) и контейнер, куда JavaScript будет добавлять записи.
        </p>
      </div>

      <h3>Полный код для <code>templates/index.html</code></h3>
      <div className="code-block">
        <code>{`<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Гостевская книга — Урок 3</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body>
    <div class="container">
        <h1>📖 Гостевская книга</h1>
        <p class="subtitle">Урок 3: SQLite — сохранение в базу данных</p>

        <form class="add-form" id="addForm">
            <input type="text" id="author" placeholder="Ваше имя" maxlength="100" required>
            <textarea id="message" placeholder="Ваше сообщение..." rows="3" maxlength="500" required></textarea>
            <button type="submit">Отправить</button>
        </form>

        <div id="entries" class="entries"></div>
    </div>

    <script src="{{ url_for('static', filename='script.js') }}"></script>
</body>
</html>`}</code>
      </div>

      <h3>🔍 Разберём HTML по строчкам:</h3>
      <ul>
        <li><code>&lt;!DOCTYPE html&gt;</code> — говорим браузеру, что это HTML5 документ</li>
        <li><code>&lt;html lang="ru"&gt;</code> — начало HTML документа, указываем язык (русский)</li>
        <li><code>&lt;head&gt;</code> — секция с метаданными (не видна пользователю):
          <ul>
            <li><code>&lt;meta charset="UTF-8"&gt;</code> — кодировка для правильного отображения русских букв</li>
            <li><code>&lt;meta name="viewport"&gt;</code> — настройка для мобильных устройств</li>
            <li><code>&lt;title&gt;</code> — заголовок страницы (виден во вкладке браузера)</li>
            <li><code>&lt;link rel="stylesheet"&gt;</code> — подключаем CSS файл со стилями. 
              <code>url_for('static', filename='style.css')</code> — это функция Flask, которая создаёт правильный путь к файлу</li>
          </ul>
        </li>
        <li><code>&lt;body&gt;</code> — видимое содержимое страницы</li>
        <li><code>&lt;div class="container"&gt;</code> — контейнер для всего содержимого страницы</li>
        <li><code>&lt;h1&gt;</code> — заголовок страницы</li>
        <li><code>&lt;p class="subtitle"&gt;</code> — подзаголовок с описанием</li>
        <li><code>&lt;form id="addForm"&gt;</code> — форма для добавления записей:
          <ul>
            <li><code>id="addForm"</code> — уникальный идентификатор, чтобы JavaScript мог найти форму</li>
            <li><code>&lt;input type="text" id="author"&gt;</code> — поле для ввода имени:
              <ul>
                <li><code>type="text"</code> — текстовое поле</li>
                <li><code>id="author"</code> — идентификатор для JavaScript</li>
                <li><code>placeholder="Ваше имя"</code> — подсказка внутри поля</li>
                <li><code>maxlength="100"</code> — максимум 100 символов</li>
                <li><code>required</code> — поле обязательно для заполнения</li>
              </ul>
            </li>
            <li><code>&lt;textarea id="message"&gt;</code> — многострочное поле для сообщения:
              <ul>
                <li><code>rows="3"</code> — высота поля (3 строки)</li>
                <li><code>maxlength="500"</code> — максимум 500 символов</li>
                <li><code>required</code> — поле обязательно</li>
              </ul>
            </li>
            <li><code>&lt;button type="submit"&gt;</code> — кнопка отправки формы. 
              <code>type="submit"</code> означает, что при нажатии форма будет отправлена</li>
          </ul>
        </li>
        <li><code>&lt;div id="entries" class="entries"&gt;</code> — контейнер для списка записей. 
          JavaScript будет добавлять записи сюда</li>
        <li><code>&lt;script src="..."&gt;&lt;/script&gt;</code> — подключаем JavaScript файл. 
          Он будет выполняться после загрузки страницы</li>
      </ul>

      <div className="info-box" style={{marginTop: '20px'}}>
        <strong>💡 Что такое Flask функция url_for?</strong>
        <p style={{marginTop: '10px'}}>
          <code>url_for('static', filename='style.css')</code> — это функция Flask, которая создаёт правильный путь к файлу. 
          Она автоматически добавляет префикс <code>/static/</code>, так что получится путь <code>/static/style.css</code>.
        </p>
        <p style={{marginTop: '10px'}}>
          Это удобно, потому что если ты изменишь структуру проекта, пути обновятся автоматически!
        </p>
      </div>

      <h3>Важные моменты</h3>
      <ul>
        <li><strong>ID элементов</strong> — каждый элемент, с которым будет работать JavaScript, должен иметь уникальный <code>id</code></li>
        <li><strong>Атрибут <code>required</code></strong> — браузер сам проверит, что поля заполнены, перед отправкой формы</li>
        <li><strong>Атрибут <code>maxlength</code></strong> — ограничивает длину текста, чтобы пользователь не мог ввести слишком длинное сообщение</li>
        <li><strong>Форма с <code>id="addForm"</code></strong> — JavaScript будет слушать событие отправки формы и обрабатывать его</li>
      </ul>

      <div className="info-box" style={{marginTop: '20px'}}>
        <strong>🎯 Как это работает:</strong>
        <ol style={{marginTop: '10px'}}>
          <li>Пользователь открывает страницу в браузере</li>
          <li>Браузер загружает HTML и видит форму</li>
          <li>Пользователь заполняет поля (имя и сообщение)</li>
          <li>Пользователь нажимает кнопку "Отправить"</li>
          <li>JavaScript "ловит" отправку формы (мы настроим это в следующем шаге)</li>
          <li>JavaScript отправляет данные на сервер через POST запрос</li>
          <li>Сервер добавляет запись в базу данных</li>
          <li>JavaScript получает ответ и добавляет запись на страницу</li>
        </ol>
      </div>

      <div className="success-box">
        <strong>✅ Проверь:</strong> 
        <ol style={{marginTop: '10px'}}>
          <li>Создай папку <code>templates</code> в папке проекта</li>
          <li>Создай файл <code>templates/index.html</code> с кодом выше</li>
          <li>Добавь маршрут в <code>app.py</code>: <code>@app.route('/') def index(): return render_template('index.html')</code></li>
          <li>Запусти сервер и открой <code>http://localhost:5002</code></li>
          <li>Ты должен увидеть форму (пока без стилей, но это нормально — стили добавим позже)</li>
        </ol>
      </div>
    </div>
  )
}

export default L3Step5
