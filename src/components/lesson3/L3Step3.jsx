function L3Step3() {
  return (
    <div className="step-content trainer-step">
      <h2>Шаг 3: Маршрут GET /api/entries — получение всех записей</h2>

      <div className="info-box trainer-definition">
        <strong>💡 Что такое GET запрос?</strong>
        <p style={{marginTop: '10px'}}>
          <strong>GET</strong> — это запрос на получение данных. Это как спросить: "Дай мне все записи из гостевой книги!"
          GET запросы не изменяют данные — они только читают их.
        </p>
        <p style={{marginTop: '10px'}}>
          Когда пользователь открывает страницу гостевой книги, JavaScript отправляет GET запрос на <code>/api/entries</code>, 
          чтобы получить все записи и показать их на странице.
        </p>
      </div>

      <h3>Код маршрута GET /api/entries</h3>
      <div className="code-block">
        <code>{`@app.route('/api/entries', methods=['GET'])
def get_entries():
    """Возвращает все записи из базы данных"""
    with get_db() as conn:
        rows = conn.execute(
            'SELECT id, author, message, created_at FROM entries ORDER BY id DESC'
        ).fetchall()
    entries = [dict(r) for r in rows]
    return jsonify({'entries': entries})`}</code>
      </div>

      <h3>🔍 Разберём код по строчкам:</h3>
      <ul>
        <li><code>@app.route('/api/entries', methods=['GET'])</code> — создаём маршрут, который принимает только GET запросы. 
          Когда браузер обратится к адресу <code>/api/entries</code>, выполнится функция <code>get_entries()</code>.</li>
        <li><code>with get_db() as conn:</code> — открываем соединение с базой данных. 
          После выполнения кода внутри <code>with</code> соединение автоматически закроется.</li>
        <li><code>conn.execute('SELECT ...')</code> — выполняем SQL запрос. 
          <code>SELECT</code> означает "выбери данные".</li>
        <li><code>SELECT id, author, message, created_at FROM entries</code> — выбираем все поля из таблицы <code>entries</code>:
          <ul>
            <li><code>id</code> — номер записи</li>
            <li><code>author</code> — имя автора</li>
            <li><code>message</code> — текст сообщения</li>
            <li><code>created_at</code> — дата создания</li>
          </ul>
        </li>
        <li><code>ORDER BY id DESC</code> — сортируем записи по номеру <code>id</code> в обратном порядке (DESC = descending). 
          Это значит, что новые записи будут первыми (у них больший номер id).</li>
        <li><code>.fetchall()</code> — получаем все строки, которые вернул запрос. 
          Результат — это список объектов Row (которые мы настроили как словари).</li>
        <li><code>entries = [dict(r) for r in rows]</code> — превращаем каждую строку в словарь Python. 
          Это называется "list comprehension" — короткий способ создать список. 
          Это то же самое, что:
          <pre style={{margin: '10px 0', padding: '10px', background: '#f5f5f5', borderRadius: '4px'}}>{`entries = []
for r in rows:
    entries.append(dict(r))`}</pre>
        </li>
        <li><code>{`return jsonify({'entries': entries})`}</code> — возвращаем данные в формате JSON. 
          Браузер получит ответ вида: <code>{`{"entries": [{"id": 1, "author": "Иван", ...}, ...]}`}</code></li>
      </ul>

      <div className="info-box" style={{marginTop: '20px'}}>
        <strong>💡 Что такое SQL запрос SELECT?</strong>
        <p style={{marginTop: '10px'}}>
          <code>SELECT</code> — это команда SQL для чтения данных из таблицы. 
          Это как сказать базе данных: "Покажи мне все записи из таблицы entries!"
        </p>
        <p style={{marginTop: '10px'}}>
          Структура запроса:
        </p>
        <ul style={{marginTop: '10px'}}>
          <li><code>SELECT</code> — что выбрать (какие поля)</li>
          <li><code>FROM</code> — из какой таблицы</li>
          <li><code>ORDER BY</code> — как отсортировать</li>
        </ul>
      </div>

      <h3>Пример ответа от сервера</h3>
      <div className="code-block">
        <code>{`{
  "entries": [
    {
      "id": 2,
      "author": "Мария",
      "message": "Отличный сайт!",
      "created_at": "2026-02-03 15:30:45"
    },
    {
      "id": 1,
      "author": "Иван",
      "message": "Привет всем!",
      "created_at": "2026-02-03 15:25:10"
    }
  ]
}`}</code>
      </div>

      <h3>Как протестировать</h3>
      <p>После запуска сервера открой в браузере:</p>
      <div className="code-block">
        <code>http://localhost:5002/api/entries</code>
      </div>
      <p>Ты увидишь JSON с массивом записей. Пока что он будет пустым <code>{`{"entries": []}`}</code>, 
        потому что мы ещё не добавили ни одной записи. В следующем шаге мы создадим маршрут для добавления записей!</p>

      <div className="info-box" style={{marginTop: '20px'}}>
        <strong>🎯 Как это работает вместе:</strong>
        <ol style={{marginTop: '10px'}}>
          <li>JavaScript на странице отправляет GET запрос на <code>/api/entries</code></li>
          <li>Flask видит запрос и вызывает функцию <code>get_entries()</code></li>
          <li>Функция открывает соединение с базой данных</li>
          <li>Выполняется SQL запрос <code>SELECT</code></li>
          <li>База данных возвращает все записи</li>
          <li>Записи превращаются в список словарей Python</li>
          <li>Список превращается в JSON и отправляется браузеру</li>
          <li>JavaScript получает данные и показывает их на странице</li>
        </ol>
      </div>

      <div className="success-box">
        <strong>✅ Проверь:</strong> 
        <ol style={{marginTop: '10px'}}>
          <li>Добавь маршрут <code>GET /api/entries</code> в свой <code>app.py</code></li>
          <li>Запусти сервер: <code>python app.py</code></li>
          <li>Открой в браузере <code>http://localhost:5002/api/entries</code></li>
          <li>Ты должен увидеть: <code>{`{"entries": []}`}</code> — пустой массив (это нормально, записей пока нет)</li>
        </ol>
      </div>
    </div>
  )
}

export default L3Step3
