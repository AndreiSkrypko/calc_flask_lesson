function L3Step1() {
  return (
    <div className="step-content trainer-step">
      <h2>Шаг 1: Подготовка проекта и знакомство с SQLite</h2>

      <div className="info-box trainer-definition">
        <strong>💡 Что такое SQLite?</strong>
        <p style={{marginTop: '10px'}}>
          <strong>SQLite</strong> — это база данных, которая хранится в одном файле на твоём компьютере. 
          Это как Excel таблица, но специально для программ. В отличие от больших баз данных (как MySQL или PostgreSQL), 
          SQLite не требует отдельного сервера — она работает прямо в твоей программе!
        </p>
        <p style={{marginTop: '10px'}}>
          Представь блокнот, где каждая страница — это таблица с данными. 
          SQLite — это такой же блокнот, только для компьютера. 
          Все данные хранятся в файле <code>guestbook.db</code>, и даже если ты закроешь программу, 
          данные останутся на месте!
        </p>
      </div>

      <h3>Что ты сделаешь</h3>
      <ol>
        <li>Создашь папку проекта <code>guestbook-flask</code></li>
        <li>Создашь виртуальное окружение и установишь Flask</li>
        <li>Создашь файл <code>app.py</code> с базовой структурой</li>
        <li>Подключишь библиотеку <code>sqlite3</code> (она уже встроена в Python!)</li>
      </ol>

      <h3>Структура проекта</h3>
      <div className="code-block">
        <code>{`guestbook-flask/
├── app.py              # Основной файл приложения
├── requirements.txt    # Зависимости проекта
├── guestbook.db        # База данных (создастся автоматически)
├── templates/
│   └── index.html      # HTML шаблон
└── static/
    ├── script.js       # JavaScript код
    └── style.css       # CSS стили`}</code>
      </div>

      <h3>Базовый код для <code>app.py</code></h3>
      <div className="code-block">
        <code>{`# Урок 3: Гостевская книга — SQLite
import sqlite3
from pathlib import Path
from flask import Flask, render_template, request, jsonify

app = Flask(__name__)
app.config['DEBUG'] = True

# Путь к файлу базы данных
DB_PATH = Path(__file__).parent / 'guestbook.db'

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5002, debug=True)`}</code>
      </div>

      <h3>🔍 Разберём код по строчкам:</h3>
      <ul>
        <li><code>import sqlite3</code> — подключаем библиотеку для работы с SQLite. Она уже встроена в Python, ничего устанавливать не нужно!</li>
        <li><code>from pathlib import Path</code> — подключаем класс Path для работы с путями к файлам. Это удобнее, чем писать пути вручную.</li>
        <li><code>DB_PATH = Path(__file__).parent / 'guestbook.db'</code> — создаём путь к файлу базы данных. 
          <code>__file__</code> — это путь к текущему файлу (app.py), 
          <code>.parent</code> — папка, где лежит app.py, 
          <code>/ 'guestbook.db'</code> — добавляем имя файла базы данных. 
          Файл будет создан в той же папке, где лежит app.py.</li>
        <li><code>app.run(port=5002, debug=True)</code> — запускаем сервер на порту 5002 (чтобы не конфликтовать с другими уроками).</li>
      </ul>

      <div className="info-box" style={{marginTop: '20px'}}>
        <strong>💡 Почему порт 5002?</strong>
        <p style={{marginTop: '10px'}}>
          В уроке 1 мы использовали порт 5000, в уроке 2 — порт 5001. 
          Теперь используем 5002, чтобы можно было запускать несколько проектов одновременно, 
          не мешая друг другу!
        </p>
      </div>

      <div className="success-box">
        <strong>✅ Проверь:</strong> 
        <ol style={{marginTop: '10px'}}>
          <li>Создай папку проекта и файл <code>app.py</code> с кодом выше</li>
          <li>Создай виртуальное окружение: <code>python -m venv venv</code></li>
          <li>Активируй его и установи Flask: <code>pip install flask</code></li>
          <li>Запусти: <code>python app.py</code></li>
          <li>Проверь, что сервер запустился без ошибок</li>
        </ol>
        <p style={{marginTop: '10px'}}>
          Пока что у нас нет маршрутов, поэтому при открытии страницы будет ошибка 404 — это нормально! 
          В следующем шаге мы создадим базу данных и таблицу.
        </p>
      </div>
    </div>
  )
}

export default L3Step1
