function L3Step4() {
  return (
    <div className="step-content trainer-step">
      <h2>Шаг 4: Маршрут POST /api/entries — добавление новой записи</h2>

      <div className="info-box trainer-definition">
        <strong>💡 Что такое POST запрос?</strong>
        <p style={{marginTop: '10px'}}>
          <strong>POST</strong> — это запрос на создание новых данных. Это как сказать: "Добавь новую запись в гостевую книгу!"
          POST запросы изменяют данные — они добавляют новую информацию в базу данных.
        </p>
        <p style={{marginTop: '10px'}}>
          Когда пользователь заполняет форму (имя и сообщение) и нажимает кнопку "Отправить", 
          JavaScript отправляет POST запрос на <code>/api/entries</code> с данными формы.
        </p>
      </div>

      <h3>Код маршрута POST /api/entries</h3>
      <div className="code-block">
        <code>{`@app.route('/api/entries', methods=['POST'])
def add_entry():
    """Добавляет новую запись в базу данных"""
    data = request.json or {}
    author = (data.get('author') or '').strip()
    message = (data.get('message') or '').strip()
    
    # Проверка: имя и сообщение не должны быть пустыми
    if not author or not message:
        return jsonify({'success': False, 'error': 'Введите имя и сообщение!'}), 400
    
    # Добавляем запись в базу данных
    with get_db() as conn:
        cur = conn.execute(
            'INSERT INTO entries (author, message) VALUES (?, ?)',
            (author, message)
        )
        conn.commit()
        # Получаем только что созданную запись
        row = conn.execute(
            'SELECT id, author, message, created_at FROM entries WHERE id = ?',
            (cur.lastrowid,)
        ).fetchone()
    
    return jsonify({'success': True, 'entry': dict(row)})`}</code>
      </div>

      <h3>🔍 Разберём код по строчкам:</h3>
      <ul>
        <li><code>@app.route('/api/entries', methods=['POST'])</code> — создаём маршрут для POST запросов. 
          Обрати внимание: тот же адрес <code>/api/entries</code>, но другой метод (<code>POST</code> вместо <code>GET</code>). 
          Flask различает их по методу!</li>
        <li><code>{`data = request.json or {}`}</code> — получаем данные из запроса. 
          <code>request.json</code> — это уже распарсенный JSON от браузера. 
          Если данных нет, используем пустой словарь <code>{`{}`}</code>.</li>
        <li><code>author = (data.get('author') or '').strip()</code> — получаем имя автора:
          <ul>
            <li><code>data.get('author')</code> — берём поле "author" из данных</li>
            <li><code>or ''</code> — если поля нет, используем пустую строку</li>
            <li><code>.strip()</code> — убираем пробелы по краям (чтобы "  Иван  " стало "Иван")</li>
          </ul>
        </li>
        <li><code>message = (data.get('message') or '').strip()</code> — то же самое для сообщения</li>
        <li><code>if not author or not message:</code> — проверяем, что оба поля заполнены. 
          Если хотя бы одно пустое — возвращаем ошибку.</li>
        <li><code>{`return jsonify({'success': False, 'error': '...'}), 400`}</code> — возвращаем ошибку с кодом 400 (Bad Request). 
          Код 400 означает "неправильный запрос" — пользователь не заполнил все поля.</li>
        <li><code>with get_db() as conn:</code> — открываем соединение с базой данных</li>
        <li><code>cur = conn.execute('INSERT INTO ...')</code> — выполняем SQL запрос INSERT для добавления записи:
          <ul>
            <li><code>INSERT INTO entries (author, message)</code> — добавляем запись в таблицу <code>entries</code>, 
              заполняем поля <code>author</code> и <code>message</code></li>
            <li><code>VALUES (?, ?)</code> — значения для полей. 
              <code>?</code> — это плейсхолдеры (заполнители), которые заменятся на реальные значения</li>
            <li><code>(author, message)</code> — реальные значения, которые подставятся вместо <code>?</code></li>
          </ul>
        </li>
        <li><code>conn.commit()</code> — сохраняем изменения в базе данных. Без этого запись не добавится!</li>
        <li><code>cur.lastrowid</code> — получаем номер (id) только что созданной записи. 
          SQLite автоматически присваивает уникальный номер каждой новой записи.</li>
        <li><code>SELECT ... WHERE id = ?</code> — получаем только что созданную запись из базы, 
          чтобы вернуть её браузеру со всеми полями (включая автоматически созданные <code>id</code> и <code>created_at</code>).</li>
        <li><code>{`return jsonify({'success': True, 'entry': dict(row)})`}</code> — возвращаем успешный ответ с новой записью. 
          Браузер получит полную информацию о созданной записи.</li>
      </ul>

      <div className="info-box" style={{marginTop: '20px'}}>
        <strong>💡 Почему используются плейсхолдеры (?) вместо прямых значений?</strong>
        <p style={{marginTop: '10px'}}>
          Это защита от SQL-инъекций — опасной атаки, когда злоумышленник может вставить вредоносный SQL код. 
          Используя <code>?</code> и передавая значения отдельно, мы гарантируем, что данные будут обработаны безопасно.
        </p>
        <p style={{marginTop: '10px'}}>
          <strong>Плохо (опасно):</strong>
        </p>
        <pre style={{margin: '10px 0', padding: '10px', background: '#ffebee', borderRadius: '4px'}}>{`query = f"INSERT INTO entries (author) VALUES ('{author}')"`}</pre>
        <p style={{marginTop: '10px'}}>
          <strong>Хорошо (безопасно):</strong>
        </p>
        <pre style={{margin: '10px 0', padding: '10px', background: '#e8f5e9', borderRadius: '4px'}}>{`conn.execute('INSERT INTO entries (author) VALUES (?)', (author,))`}</pre>
      </div>

      <h3>Пример запроса от браузера</h3>
      <div className="code-block">
        <code>{`POST /api/entries
Content-Type: application/json

{
  "author": "Иван",
  "message": "Привет всем! Отличный сайт!"
}`}</code>
      </div>

      <h3>Пример ответа от сервера</h3>
      <div className="code-block">
        <code>{`{
  "success": true,
  "entry": {
    "id": 1,
    "author": "Иван",
    "message": "Привет всем! Отличный сайт!",
    "created_at": "2026-02-03 15:30:45"
  }
}`}</code>
      </div>

      <h3>Как протестировать вручную</h3>
      <p>Можно использовать curl или Postman:</p>
      <div className="code-block">
        <code>{`curl -X POST http://localhost:5002/api/entries \\
  -H "Content-Type: application/json" \\
  -d '{"author":"Иван","message":"Привет!"}'`}</code>
      </div>

      <div className="info-box" style={{marginTop: '20px'}}>
        <strong>🎯 Как это работает вместе:</strong>
        <ol style={{marginTop: '10px'}}>
          <li>Пользователь заполняет форму (имя и сообщение) и нажимает "Отправить"</li>
          <li>JavaScript отправляет POST запрос на <code>/api/entries</code> с данными формы</li>
          <li>Flask получает запрос и вызывает функцию <code>add_entry()</code></li>
          <li>Функция проверяет, что все поля заполнены</li>
          <li>Если всё хорошо — открывается соединение с базой данных</li>
          <li>Выполняется SQL запрос <code>INSERT</code> — новая запись добавляется в таблицу</li>
          <li>Изменения сохраняются (<code>commit</code>)</li>
          <li>Получаем созданную запись из базы (со всеми полями)</li>
          <li>Возвращаем ответ браузеру с новой записью</li>
          <li>JavaScript получает ответ и добавляет запись на страницу</li>
        </ol>
      </div>

      <div className="success-box">
        <strong>✅ Проверь:</strong> 
        <ol style={{marginTop: '10px'}}>
          <li>Добавь маршрут <code>POST /api/entries</code> в свой <code>app.py</code></li>
          <li>Не забудь импортировать <code>request</code>: <code>from flask import request</code></li>
          <li>Запусти сервер: <code>python app.py</code></li>
          <li>Попробуй отправить POST запрос через curl или Postman</li>
          <li>Проверь, что запись добавилась: открой <code>GET /api/entries</code> — там должна быть новая запись!</li>
        </ol>
      </div>
    </div>
  )
}

export default L3Step4
