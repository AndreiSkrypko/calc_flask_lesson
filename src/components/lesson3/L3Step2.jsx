function L3Step2() {
  return (
    <div className="step-content trainer-step">
      <h2>Шаг 2: Создание таблицы и функции работы с БД</h2>

      <div className="info-box trainer-definition">
        <strong>💡 Что такое таблица в базе данных?</strong>
        <p style={{marginTop: '10px'}}>
          Представь таблицу в Excel: у неё есть столбцы (колонки) и строки. 
          В базе данных то же самое:
        </p>
        <ul style={{marginTop: '10px'}}>
          <li><strong>Столбцы</strong> — это поля (например, "имя", "сообщение", "дата")</li>
          <li><strong>Строки</strong> — это записи (каждая запись — это одно сообщение в гостевой книге)</li>
        </ul>
        <p style={{marginTop: '10px'}}>
          Мы создадим таблицу <code>entries</code> (записи) с полями: 
          <code>id</code>, <code>author</code>, <code>message</code>, <code>created_at</code>.
        </p>
      </div>

      <h3>1. Функция для подключения к базе данных</h3>
      <div className="code-block">
        <code>{`def get_db():
    """Открывает соединение с базой данных и возвращает его"""
    conn = sqlite3.connect(DB_PATH)
    conn.row_factory = sqlite3.Row  # Чтобы строки возвращались как словари
    return conn`}</code>
      </div>

      <h3>🔍 Разберём функцию <code>get_db()</code>:</h3>
      <ul>
        <li><code>sqlite3.connect(DB_PATH)</code> — открываем соединение с базой данных. 
          Если файл <code>guestbook.db</code> не существует, SQLite создаст его автоматически!</li>
        <li><code>conn.row_factory = sqlite3.Row</code> — настраиваем, чтобы строки из базы возвращались как словари. 
          Это удобно: вместо <code>row[0]</code> можно писать <code>row['id']</code>.</li>
        <li><code>return conn</code> — возвращаем соединение, чтобы использовать его дальше.</li>
      </ul>

      <h3>2. Функция для создания таблицы</h3>
      <div className="code-block">
        <code>{`def init_db():
    """Создаёт таблицу entries, если её ещё нет"""
    with get_db() as conn:
        conn.execute('''
            CREATE TABLE IF NOT EXISTS entries (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                author TEXT NOT NULL,
                message TEXT NOT NULL,
                created_at TEXT DEFAULT (datetime('now'))
            )
        ''')
        conn.commit()`}</code>
      </div>

      <h3>🔍 Разберём функцию <code>init_db()</code> и SQL запрос:</h3>
      <ul>
        <li><code>with get_db() as conn:</code> — используем контекстный менеджер. 
          Когда код внутри <code>with</code> закончится, соединение автоматически закроется. 
          Это как открыть дверь, войти, и дверь сама закроется за тобой!</li>
        <li><code>CREATE TABLE IF NOT EXISTS entries</code> — создаём таблицу с именем <code>entries</code>, 
          но только если её ещё нет. Если таблица уже существует, ничего не произойдёт (не будет ошибки).</li>
        <li><code>id INTEGER PRIMARY KEY AUTOINCREMENT</code> — поле <code>id</code>:
          <ul>
            <li><code>INTEGER</code> — целое число</li>
            <li><code>PRIMARY KEY</code> — главный ключ (каждая запись имеет уникальный номер)</li>
            <li><code>AUTOINCREMENT</code> — автоматически увеличивается (1, 2, 3, 4...)</li>
          </ul>
        </li>
        <li><code>author TEXT NOT NULL</code> — поле для имени автора:
          <ul>
            <li><code>TEXT</code> — текст</li>
            <li><code>NOT NULL</code> — обязательно должно быть заполнено (не может быть пустым)</li>
          </ul>
        </li>
        <li><code>message TEXT NOT NULL</code> — поле для сообщения (тоже текст, обязательно)</li>
        <li><code>created_at TEXT DEFAULT (datetime('now'))</code> — поле для даты создания:
          <ul>
            <li><code>DEFAULT (datetime('now'))</code> — если не указать дату, автоматически поставится текущая дата и время</li>
          </ul>
        </li>
        <li><code>conn.commit()</code> — сохраняем изменения в базе данных. 
          Без этого изменения не применятся!</li>
      </ul>

      <div className="info-box" style={{marginTop: '20px'}}>
        <strong>💡 Что такое SQL?</strong>
        <p style={{marginTop: '10px'}}>
          <strong>SQL</strong> (Structured Query Language) — это язык для работы с базами данных. 
          Это как специальный язык, на котором ты "разговариваешь" с базой данных:
        </p>
        <ul style={{marginTop: '10px'}}>
          <li><code>CREATE TABLE</code> — "Создай таблицу"</li>
          <li><code>SELECT</code> — "Покажи мне данные"</li>
          <li><code>INSERT</code> — "Добавь новую запись"</li>
          <li><code>UPDATE</code> — "Измени запись"</li>
          <li><code>DELETE</code> — "Удали запись"</li>
        </ul>
      </div>

      <h3>3. Вызов <code>init_db()</code> при запуске</h3>
      <div className="code-block">
        <code>{`if __name__ == '__main__':
    init_db()  # Создаём таблицу при первом запуске
    app.run(host='0.0.0.0', port=5002, debug=True)`}</code>
      </div>

      <p>Теперь при каждом запуске приложения будет проверяться, существует ли таблица, и если нет — она создастся автоматически!</p>

      <div className="info-box" style={{marginTop: '20px'}}>
        <strong>🎯 Как это работает:</strong>
        <ol style={{marginTop: '10px'}}>
          <li>Ты запускаешь <code>python app.py</code></li>
          <li>Python выполняет код в блоке <code>if __name__ == '__main__':</code></li>
          <li>Вызывается функция <code>init_db()</code></li>
          <li>Функция открывает соединение с базой данных (или создаёт файл, если его нет)</li>
          <li>Выполняется SQL запрос <code>CREATE TABLE IF NOT EXISTS</code></li>
          <li>Если таблицы нет — она создаётся, если есть — ничего не происходит</li>
          <li>Изменения сохраняются (<code>commit</code>)</li>
          <li>Соединение закрывается автоматически</li>
          <li>Сервер запускается</li>
        </ol>
      </div>

      <div className="success-box">
        <strong>✅ Проверь:</strong> 
        <ol style={{marginTop: '10px'}}>
          <li>Добавь функции <code>get_db()</code> и <code>init_db()</code> в свой <code>app.py</code></li>
          <li>Добавь вызов <code>init_db()</code> перед запуском сервера</li>
          <li>Запусти: <code>python app.py</code></li>
          <li>Проверь, что в папке проекта появился файл <code>guestbook.db</code></li>
          <li>Если файл появился — всё работает! Таблица создана.</li>
        </ol>
      </div>
    </div>
  )
}

export default L3Step2
