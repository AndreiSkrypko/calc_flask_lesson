# 🚀 Быстрый старт

## Для учеников - Интерактивное руководство

1. Откройте терминал в папке проекта (файлы уже в корне!)
2. Установите зависимости:
   ```bash
   npm install
   ```
3. Запустите приложение:
   ```bash
   npm run dev
   ```
4. Откройте браузер по адресу, который покажет терминал (обычно http://localhost:5173)
5. Следуйте пошаговым инструкциям на экране

**Или используйте онлайн версию:** Проект автоматически деплоится на Vercel!

## Для проверки готового проекта

1. Откройте терминал в папке проекта
2. Перейдите в папку calc+flask:
   ```bash
   cd calc+flask
   ```
3. Создайте виртуальное окружение (опционально, но рекомендуется):
   ```bash
   python -m venv venv
   venv\Scripts\activate
   ```
4. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
5. Запустите Flask сервер:
   ```bash
   python app.py
   ```
6. Откройте браузер: http://localhost:5000

## Структура проекта

```
flask_lessons/
├── src/                      # Исходный код React приложения
│   └── components/          # Компоненты уроков (11 шагов)
├── public/                   # Статические файлы
├── calc+flask/              # Готовый проект с комментариями
│   ├── app.py               # Flask приложение
│   ├── templates/           # HTML шаблоны
│   └── static/              # CSS и JavaScript
├── package.json             # Зависимости Node.js
├── vercel.json              # Конфигурация Vercel
└── README.md                # Подробная документация
```

## Что делать дальше?

1. **Для обучения:** Запустите React приложение (`npm run dev`) - следуйте шагам и создавайте проект в PyCharm
2. **Для проверки:** Откройте `calc+flask` - посмотрите готовый код с комментариями
3. **Для сравнения:** Сравните свой код с эталонным в `calc+flask`
4. **Онлайн:** Проект автоматически доступен на Vercel после каждого push в GitHub!

Удачи! 🎉
