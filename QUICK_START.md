# 🚀 Быстрый старт

## Для учеников - Интерактивное руководство

1. Откройте терминал в папке проекта
2. Перейдите в папку react-tutorial:
   ```bash
   cd react-tutorial
   ```
3. Установите зависимости:
   ```bash
   npm install
   ```
4. Запустите приложение:
   ```bash
   npm run dev
   ```
5. Откройте браузер по адресу, который покажет терминал (обычно http://localhost:5173)
6. Следуйте пошаговым инструкциям на экране

## Для проверки готового проекта

1. Откройте терминал в папке проекта
2. Перейдите в папку calc+flask:
   ```bash
   cd calc+flask
   ```
3. Создайте виртуальное окружение (опционально, но рекомендуется):
   ```bash
   python -m venv venv
   venv\Scripts\activate
   ```
4. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
5. Запустите Flask сервер:
   ```bash
   python app.py
   ```
6. Откройте браузер: http://localhost:5000

## Структура проекта

```
flask_lessons/
├── react-tutorial/          # Интерактивное руководство (React)
│   └── src/components/      # 10 шагов урока
├── calc+flask/              # Готовый проект с комментариями
│   ├── app.py               # Flask приложение
│   ├── templates/           # HTML шаблоны
│   └── static/              # CSS и JavaScript
└── README.md                # Подробная документация
```

## Что делать дальше?

1. **Для обучения:** Используйте `react-tutorial` - следуйте шагам и создавайте проект в PyCharm
2. **Для проверки:** Откройте `calc+flask` - посмотрите готовый код с комментариями
3. **Для сравнения:** Сравните свой код с эталонным в `calc+flask`

Удачи! 🎉
