# 🪪 Урок 4: Сайт-визитка

**Несколько страниц**, **базовый шаблон** и **навигация**. Без базы данных — только HTML и Flask.

## Что нового

- Маршруты: `/`, `/about`, `/contact`.
- **Наследование шаблонов**: общий `base.html` с навбаром и футером, страницы расширяют его через `{% extends %}` и `{% block %}`.
- Навигация через `url_for()` по именам маршрутов.

## Как запустить

```bash
cd lesson4/visitor-flask
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

Откройте http://localhost:5003

## Структура

```
lesson4/
├── README.md
└── visitor-flask/
    ├── app.py
    ├── requirements.txt
    ├── templates/
    │   ├── base.html
    │   ├── index.html
    │   ├── about.html
    │   └── contact.html
    └── static/
        └── style.css
```

## Что сделать самому

1. Создать `base.html` с общей разметкой, `<nav>`, `{% block content %}` и `{% block title %}`.
2. Сделать отдельные шаблоны для главной, «Обо мне», «Контакты» с `{% extends 'base.html' %}`.
3. В `app.py` — по маршруту на каждую страницу, `render_template(...)`.
4. В навбаре использовать `url_for('index')`, `url_for('about')`, `url_for('contact')`.
