# 📝 Урок 5: Простой блог

**CRUD** постов: создание, просмотр, редактирование, удаление. **SQLite**, **формы**, **валидация**, **redirect** и **flash‑сообщения**.

## Что нового

- Таблица `posts`: `id`, `title`, `body`, `created_at`.
- Маршруты: список постов, просмотр одного, форма создания, форма редактирования, удаление (POST).
- Валидация: непустые заголовок и текст, ограничение длины.
- Формы через `request.form`, ответ — `redirect` и `flash`.

## Как запустить

```bash
cd lesson5/blog-flask
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

Откройте http://localhost:5004

## Структура

```
lesson5/
├── README.md
└── blog-flask/
    ├── app.py
    ├── requirements.txt
    ├── templates/
    │   ├── base.html
    │   ├── index.html
    │   ├── view.html
    │   └── form.html
    └── static/
        └── style.css
```

После запуска появится `blog.db`.

## Что сделать самому

1. Создать таблицу `posts`, подключить SQLite.
2. Реализовать CRUD: список, просмотр, создание, редактирование, удаление.
3. Формы с `method="post"`, обработка `request.form`, валидация.
4. При ошибках валидации — показывать форму снова с `flash`; при успехе — `redirect` на страницу поста или на список.
5. Использовать `get_flashed_messages()` в базовом шаблоне для вывода сообщений.
