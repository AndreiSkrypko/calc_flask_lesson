# 🚀 Быстрый старт — Урок 1

## Интерактивное руководство (урок)

1. Откройте терминал в папке **lesson1**
2. Установите зависимости:
   ```bash
   npm install
   ```
3. Запустите приложение:
   ```bash
   npm run dev
   ```
4. Откройте браузер по адресу из терминала (обычно http://localhost:5173)
5. Следуйте пошаговым инструкциям на экране

## Проверка готового калькулятора (calc+flask)

1. Откройте терминал в папке **lesson1**
2. Перейдите в папку с готовым проектом:
   ```bash
   cd calc+flask
   ```
3. (Опционально) Создайте виртуальное окружение:
   ```bash
   python -m venv venv
   venv\Scripts\activate
   ```
4. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
5. Запустите Flask:
   ```bash
   python app.py
   ```
6. Откройте в браузере: http://localhost:5000

## Структура lesson1

```
lesson1/
├── src/                 # React-урок (11 шагов)
├── public/
├── calc+flask/          # Готовый калькулятор на Flask
│   ├── app.py
│   ├── templates/
│   └── static/
├── package.json
└── ...
```

## Дальнейшие шаги

1. **Учёба:** `npm run dev` в lesson1 → выполняйте шаги и создавайте свой проект в PyCharm
2. **Проверка:** Запустите `calc+flask` и сравните с эталонным кодом

Удачи! 🎉
