# 🚀 Деплой на Vercel

Инструкция по развертыванию React приложения на Vercel.

## 📋 Что было исправлено

Создан файл `vercel.json` для правильной работы SPA (Single Page Application) на Vercel.

## 🔧 Конфигурация Vercel

Файл `vercel.json` содержит:
- **rewrites** - перенаправляет все запросы на `index.html` (необходимо для React Router/SPA)
- **buildCommand** - команда для сборки проекта
- **outputDirectory** - папка с собранными файлами (для Vite это `dist`)
- **framework** - указание на использование Vite

## 🚀 Как задеплоить

### Способ 1: Через веб-интерфейс Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. Нажмите **"New Project"**
4. Выберите репозиторий `calc_flask_lesson`
5. В настройках проекта:
   - **Root Directory**: выберите `react-tutorial` (если проект в подпапке)
   - **Framework Preset**: выберите **Vite**
   - **Build Command**: `npm run build` (должно быть автоматически)
   - **Output Directory**: `dist` (должно быть автоматически)
6. Нажмите **"Deploy"**

### Способ 2: Через Vercel CLI

1. Установите Vercel CLI:
   ```bash
   npm i -g vercel
   ```

2. Перейдите в папку react-tutorial:
   ```bash
   cd react-tutorial
   ```

3. Запустите деплой:
   ```bash
   vercel
   ```

4. Следуйте инструкциям в терминале

### Способ 3: Автоматический деплой через GitHub

1. Подключите репозиторий к Vercel (см. Способ 1)
2. Vercel автоматически будет деплоить при каждом push в main ветку

## ⚙️ Настройки проекта на Vercel

Если проект уже задеплоен, но не работает:

1. Зайдите в настройки проекта на Vercel
2. Перейдите в **Settings → General**
3. Проверьте:
   - **Root Directory**: `react-tutorial` (если проект в подпапке)
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
   - **Install Command**: `npm install`

4. В разделе **Settings → Git** убедитесь, что указана правильная ветка (обычно `main`)

## 🔍 Проверка деплоя

После деплоя проверьте:

1. ✅ Главная страница открывается без ошибок
2. ✅ Навигация между шагами работает
3. ✅ Нет ошибок 404 при обновлении страницы
4. ✅ Все стили загружаются правильно

## 🐛 Решение проблем

### Ошибка 404 на всех страницах
- Убедитесь, что файл `vercel.json` находится в папке `react-tutorial`
- Проверьте, что в настройках Vercel указан правильный Root Directory

### Ошибка при сборке
- Проверьте, что все зависимости установлены (`npm install`)
- Убедитесь, что версия Node.js совместима (Vercel использует Node.js 18+)

### Стили не загружаются
- Проверьте пути к файлам в `index.html`
- Убедитесь, что все статические файлы находятся в папке `public`

## 📝 Дополнительная информация

- Документация Vercel: https://vercel.com/docs
- Документация Vite: https://vitejs.dev/guide/static-deploy.html#vercel
