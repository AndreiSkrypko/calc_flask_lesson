# ⚙️ Настройка Vercel для проекта в подпапке

## 🔧 Проблема

Проект React находится в подпапке `react-tutorial`, поэтому Vercel нужно указать правильный путь.

## ✅ Решение

### Вариант 1: Настройка через веб-интерфейс Vercel (РЕКОМЕНДУЕТСЯ)

1. Зайдите на [vercel.com](https://vercel.com) и откройте ваш проект
2. Перейдите в **Settings → General**
3. Найдите раздел **Root Directory**
4. Нажмите **Edit** и укажите: `react-tutorial`
5. Сохраните изменения
6. Перейдите в **Deployments** и нажмите **Redeploy** на последнем деплое

### Вариант 2: Использование vercel.json в корне

Файл `vercel.json` в корне репозитория уже настроен для работы с подпапкой.

**Важно:** После изменений нужно сделать **Redeploy** проекта на Vercel!

## 🚀 Шаги для исправления ошибки 404

1. **Убедитесь, что файл `vercel.json` есть в корне репозитория**
   - Он должен содержать правильные пути к `react-tutorial`

2. **Проверьте настройки проекта на Vercel:**
   - Root Directory: `react-tutorial`
   - Build Command: `npm run build` (или оставить пустым - будет использоваться из package.json)
   - Output Directory: `dist`
   - Install Command: `npm install` (или оставить пустым)

3. **Сделайте Redeploy:**
   - Зайдите в Deployments
   - Нажмите на три точки рядом с последним деплоем
   - Выберите **Redeploy**

4. **Проверьте логи сборки:**
   - Если есть ошибки, они будут видны в логах деплоя

## 📝 Структура файлов

```
flask_lessons/
├── vercel.json          ← Конфигурация для Vercel (в корне)
├── react-tutorial/
│   ├── vercel.json      ← Конфигурация для маршрутизации SPA
│   ├── package.json
│   ├── vite.config.js
│   └── dist/           ← Собранные файлы (создаются при build)
```

## 🔍 Проверка

После деплоя проверьте:
- ✅ Главная страница открывается
- ✅ Нет ошибки 404
- ✅ Навигация работает
- ✅ Все стили загружаются

## 🐛 Если ошибка сохраняется

1. Убедитесь, что в настройках Vercel указан **Root Directory: `react-tutorial`**
2. Проверьте, что файл `vercel.json` в корне содержит правильные пути
3. Убедитесь, что файл `react-tutorial/vercel.json` существует
4. Проверьте логи сборки на наличие ошибок
5. Попробуйте очистить кеш и сделать новый деплой

## 💡 Альтернативное решение

Если проблема не решается, можно:
1. Создать отдельный репозиторий только для `react-tutorial`
2. Или использовать монорепозиторий с правильной настройкой Vercel
