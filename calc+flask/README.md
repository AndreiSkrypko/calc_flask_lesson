# 🧮 Калькулятор на Flask

Это готовый проект калькулятора на Flask с подробными комментариями.

## 📋 Что это за проект?

Это веб-калькулятор, который позволяет выполнять основные математические операции:
- ➕ Сложение
- ➖ Вычитание  
- ✖️ Умножение
- ➗ Деление

## 🚀 Как запустить проект?

### Шаг 1: Установка зависимостей

Откройте терминал в PyCharm и выполните:

```bash
pip install -r requirements.txt
```

Эта команда установит все необходимые библиотеки (Flask и его зависимости).

### Шаг 2: Запуск сервера

Выполните команду:

```bash
python app.py
```

Или в PyCharm:
1. Откройте файл `app.py`
2. Нажмите правой кнопкой мыши на файл
3. Выберите "Run 'app'"

### Шаг 3: Открыть в браузере

После запуска вы увидите сообщение:
```
 * Running on http://127.0.0.1:5000
```

Откройте этот адрес в браузере, и вы увидите калькулятор!

## 📁 Структура проекта

```
calc+flask/
├── app.py              # Главный файл Flask приложения
├── requirements.txt    # Список зависимостей
├── README.md          # Этот файл
├── templates/         # HTML шаблоны
│   └── index.html     # Главная страница калькулятора
└── static/            # Статические файлы (CSS, JS)
    ├── style.css      # Стили для страницы
    └── script.js      # JavaScript код для интерактивности
```

## 📚 Что изучается в этом проекте?

1. **Flask** - создание веб-сервера
2. **HTML** - структура веб-страницы
3. **CSS** - стилизация страницы
4. **JavaScript** - интерактивность и работа с API
5. **HTTP запросы** - GET и POST запросы
6. **JSON** - формат обмена данными

## 💡 Как это работает?

1. Пользователь вводит два числа и выбирает операцию
2. JavaScript отправляет POST запрос на сервер (`/calculate`)
3. Flask обрабатывает запрос, выполняет вычисление
4. Сервер возвращает результат в формате JSON
5. JavaScript получает результат и отображает его на странице

## 🎓 Для кого этот проект?

Этот проект создан для обучения детей основам веб-разработки с Flask.
Все файлы содержат подробные комментарии, объясняющие каждую строку кода.
